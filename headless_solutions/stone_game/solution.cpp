class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // Alice can always win by choosing the set of piles (even or odd indices)
        // that has the larger sum.
        return true;
    }
};
