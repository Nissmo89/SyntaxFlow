/**
 * @param {number[]} piles
 * @return {boolean}
 */
var stoneGame = function(piles) {
    // Alice always wins by choosing the larger sum of either 
    // all even-indexed or all odd-indexed piles.
    return true;
};
